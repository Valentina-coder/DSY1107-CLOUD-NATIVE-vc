{
  "name": "dsy1107-cloud-native",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "backend": "cd Backend && ./mvnw spring-boot:run",
    "frontend": "npm run dev --prefix frontend",
    "dev": "concurrently \"npm run backend\" \"npm run frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
